# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit real secrets.

AI_API_KEY=your-api-key-here

# Kokoro TTS Gateway (optional — falls back to system TTS)
KOKORO_TTS_URL=https://kokoro-tts-gateway.fly.dev
KOKORO_TTS_API_KEY=your-kokoro-api-key-here
